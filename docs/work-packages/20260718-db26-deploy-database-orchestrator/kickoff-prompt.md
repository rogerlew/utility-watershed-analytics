# Kickoff prompt — DB26 database deployment orchestrator

Execute `package.md` to its honest terminal state from commit `c89a22e` on
`agent/database-backup-deployment-spec`.

Use repository changes, synthetic read-only release/plan inputs, fake secrets,
bounded fixture adapters, disposable PostGIS/containers, and forest1-local
private operation/report/backup directories only. Do not access `wepp3`, use
production secrets or real release inputs, activate or roll back production,
install/start a system unit, dispatch a workflow, commit, push, open a PR, or
mutate external systems.

Keep DB20–DB25 rules in Python/Django. The shell orchestrator owns only host
locking, durable state, verified backup/report paths, phase ordering, resume,
signals, worker refresh, and cleanup.
