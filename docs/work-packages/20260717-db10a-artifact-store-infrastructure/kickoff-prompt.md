# Kickoff prompt — DB10A forest1 artifact backup acceptance

Execute `package.md` on `forest1` using
`/wc1/utility-watershed-analytics-artifacts/v1` as the authorized artifact backup
root. Keep `/wc1/utility-watershed-analytics-db-backups` separate and unchanged.

Use one operator, private filesystem modes, SHA-256 content keys, verified
atomic copies, three retained test releases, and an exact clean restore. Do not
select or contact a provider, create credentials, access production, commit, or
push. Finish only after the real path passes the package gates.
