# Kickoff prompt — DB27 protected release workflow

Execute `package.md` to its honest terminal state from commit `fde1d7b` on
`agent/database-backup-deployment-spec`.

Use repository changes, synthetic immutable request/report/inventory/status
fixtures, fake role/event data, workflow static validation, and disposable
forest1 state only. Do not change GitHub settings or secrets, dispatch a
workflow, publish an artifact, access `wepp3`, use real releases or production
secrets, install/start DB26's unit, activate/rollback production, commit, push,
open a PR, or mutate another external system.

Keep this simple for one operator. GitHub environments provide sequential
approval gates; the repository provides immutable hash binding, distinct
deploy/rollback entry points, aggregate public status, private monitoring, and
durable sanitized reports. A merge must never deploy watershed data.
