# Kickoff prompt — DB29 Mill Creek re-vendoring

Execute `docs/work-packages/20260718-db29-mill-creek-rhessys-revendor/package.md`
from `forest1` at starting revision `0b7021c`.

Lock successor run `some-oligopoly` only. Establish its exact public project
directory, publish all six ordinary inputs immutably to the operator-owned
`/wc1` production artifact namespace, and re-vendor only a complete observed
precomputed RHESSys matrix. Preserve stable key `mill-creek` and former run
`mdobre-invincible-scarab` as lineage, never as a fallback data source.

Stop on missing/protected/inferred RHESSys data. An honest
`EXECUTED-HOLD-RHESSYS-SOURCE` with accepted ordinary artifacts is preferable
to an empty or fabricated capability. Do not access `wepp3`, mutate a database,
activate/adopt a release, change public serving, select a provider, delete
anything, dispatch a workflow, commit/push DB29, or open a PR.
