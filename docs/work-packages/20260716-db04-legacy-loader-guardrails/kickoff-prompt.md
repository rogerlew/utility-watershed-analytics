# DB04 Kickoff Prompt

Do not execute until DB02 is complete and the package records an exact starting
revision and repository authority. Production deployment remains separate and
follows DB03.

Read `AGENTS.md`, the DB04 package, current loader command, Django settings and
URLs, production Compose, architecture Phase 0, and deployment guide. Preserve
all local work. Reject destructive flags before any deletion. Do not use a
backup as permission to test destructive behavior in production.
