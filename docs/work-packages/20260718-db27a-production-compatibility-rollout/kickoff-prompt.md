# Kickoff prompt — DB27A production compatibility rollout

Execute `package.md` to its honest terminal state from commit `5b358c1` on
`agent/database-backup-deployment-spec`.

Use `forest1` for repository work, the encrypted backup repository under
`/wc1/utility-watershed-analytics-db-backups`, and disposable isolated
production-shaped rehearsal resources. On `wepp3`, stay within the exact
read-only preflight, fresh backup, DB25 role/migration-credential convergence,
additive one-shot migration, dual-compatible application deployment, health
verification, and cleanup boundary in the package. Hold the canonical exclusive
operations lock for production mutation.

Do not adopt or activate a release, change existing serving data beyond
reviewed additive migration backfills, upgrade PostgreSQL/PostGIS, replace the
database volume, reboot, dispatch a data-release workflow, commit/push DB27A,
open a PR, or repair an unrelated discrepancy. Stop and terminalize honestly if
any frozen identity, backup, rehearsal, lock, role, migration, deployment, or
invariant gate fails.
