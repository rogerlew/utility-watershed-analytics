# DB31 kickoff prompt

Execute only the bounded DB31 package in this directory on forest1. Build the
129-member target from accepted DB28–DB30 inputs and existing operator-owned
`/wc1` bytes, with `some-oligopoly` ordinary WEPP data but no Mill Creek
RHESSYS and with Bremerton04 excluded. Produce two independent exact clean
builds and bound plans. Do not access wepp3, activate production, select a
provider, delete artifacts, execute DB32, commit, push, open a PR, or dispatch
a workflow.
