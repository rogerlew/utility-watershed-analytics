# Kickoff prompt — DB23 atomic desired-state reconciler

Execute `package.md` to its honest terminal state from commit `4841648` on
`agent/database-backup-deployment-spec`.

Use repository changes, synthetic release/domain fixtures, complete DB20
staging, and disposable PostGIS on forest1 only. Do not access `wepp3`, real
releases/artifacts/plans, commit, push, open a PR, or dispatch a workflow.

Reuse the DB20 writer and DB22 plan. Reassert the base under both database
locks, preserve retained public identities, mutate only exact plan scope, and
advance the pointer only with the complete accepted target in one transaction.
