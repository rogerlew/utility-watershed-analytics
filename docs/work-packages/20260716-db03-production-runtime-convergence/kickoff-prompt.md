# DB03 Kickoff Prompt

Do not execute this package until
`docs/work-packages/20260716-db03-production-runtime-convergence/package.md`
records DB01/DB02 completion, exact starting revision, read authority, mutation
authority, frozen production identities, named operator/reviewer, and exact
commands. Read `AGENTS.md` and both production runbooks first.

Never invoke the loaded legacy stop path, `docker compose down`, a project
switch, or any database create/recreate/replace/stop/remove/mount action. A
database identity difference is a terminal hold, not a reason to improvise.
