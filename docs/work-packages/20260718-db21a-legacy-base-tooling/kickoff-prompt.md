# Kickoff prompt — DB21A legacy-base tooling

Execute `package.md` to its honest terminal state from commit `2d6eacb` on
`agent/database-backup-deployment-spec`.

Use repository changes, synthetic fixtures, temporary content-addressed
artifacts, and disposable PostGIS on forest1 only. Do not access `wepp3`, real
production data/artifacts, commit, push, open a PR, or dispatch a workflow.

Reuse DB20 artifacts/writer and DB21 fingerprints. Adoption must not rewrite
pre-existing domain rows. Rollback may delete only the exact reviewed bootstrap
capabilities and must restore coherent `EMPTY` plus the bounded legacy fallback.
